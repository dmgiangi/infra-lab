---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: keycloak
  namespace: identity
spec:
  parentRefs:
    - name: kong
      namespace: ingress
      sectionName: https
  hostnames:
    - auth.calcifer.tech
  rules:
    - backendRefs:
        - name: identity-keycloak
          port: 80
